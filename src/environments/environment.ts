// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.


export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080'
};

/*
    https://angular.io/guide/build
    this.user.id = 9999,
    this.user.username = 'Roy Batty';
    this.user.password = 'NEXUS-6';
    this.user.email = 'rutgerhauer@earth.com.br';
    this.user.mobile = '61 998887777';
    this.user.name = 'Rutger Hauer';
  
    “I’ve seen things you people wouldn’t believe” dying speech… 
    “All those moments will be lost in time like tears in the rain. Time to die.”
  */
  
  /*
   * For easier debugging in development mode, you can import the following file
   * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
   *
   * This import should be commented out in production mode because it will have a negative impact
   * on performance if an error is thrown.
   */
  // import 'zone.js/plugins/zone-error';  // Included with Angular CLI.

